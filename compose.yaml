services:
  auction:
    build: .
    ports:
      - "8000:8000"
    environment:
      RIOT_API_KEY: ${RIOT_API_KEY:-}
      DATA_FILE: /data/state.json
      SESSION_SECRET: ${SESSION_SECRET:-local-development-secret}
      SCRIM_ADMIN_PASSWORD: ${SCRIM_ADMIN_PASSWORD:-1234}
    volumes:
      - auction-data:/data
    restart: unless-stopped

volumes:
  auction-data:
